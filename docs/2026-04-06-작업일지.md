# 작업 일지 - 2026년 4월 6일

## 요약

- 세목별 담당자 관리 예산금액 비율(배분율/사용율) 표시
- 세목별 담당자 관리 계층별 합계 표시 (위원회/사역팀/항/목)
- 세목별 사용금액을 지급완료 지출결의서 합계로 실시간 계산
- budget-view 페이지에도 동일한 사용금액 계산 로직 적용
- 연도별 역할(UserYearRole) 기반 권한 검사로 변경

---

## 완료된 작업

### 1. 세목별 담당자 관리 - 예산금액 비율 표시

**커밋:** `8084899` feat: 세목별 담당자 관리 예산금액 비율 표시 기능 추가

**기능:**
- **배분율**: 세목 예산금액 / 전체 예산금액 (%)
- **사용율**: 사용금액 / 예산금액 (%)
- 사용율 색상 표시: 80% 이상 빨간색, 50-80% 주황색

**추가 함수:**
- `formatPercent()` - 비율 포맷
- `getUsageColorClass()` - 사용율 색상 클래스
- `totalBudget` - 전체 예산금액 합계 계산

---

### 2. 세목별 담당자 관리 - 계층별 합계 표시

**커밋:** `19efa0e` feat: 세목별 담당자 관리 계층별 합계 및 배분율 표시

**기능:**
각 계층(위원회/사역팀/항/목) 헤더에 합계 표시:
- 예산금액 합계
- 배분율 (총 예산 대비)
- 사용금액 합계
- 사용율 (색상 구분)

**추가 함수:**
- `calculateTotals()` - 세목 배열에서 예산/사용 합계 계산

---

### 3. 세목별 사용금액 - 지급완료 지출결의서 합계로 계산

**커밋:** `0d010ed` feat: 세목별 사용금액을 지급완료 지출결의서 합계로 계산

**변경 내용:**
- 기존: `BudgetDetailYear.usedAmount` 저장값 반환 (항상 0)
- 변경: `ExpenseItem`을 `budgetDetail`별로 그룹화하여 실시간 계산

**조건:**
- `expense.paymentStatus = 'COMPLETED'`
- `expense.expenseDate`가 해당 연도에 속하는 항목만 필터

**수정 파일:**
- `app/api/budget-details/year/route.ts`

---

### 4. budget-view 페이지 - 동일한 사용금액 계산 적용

**커밋:** `7c4114c` feat: budget-view 페이지 사용금액도 지급완료 지출결의서 합계로 계산

**수정 파일:**
- `app/api/budget/hierarchy/route.ts`

동일한 로직 적용:
```typescript
const usedAmounts = await prisma.expenseItem.groupBy({
  by: ['budgetDetail'],
  where: {
    expense: {
      paymentStatus: 'COMPLETED',
      expenseDate: { gte: new Date(year, 0, 1), lt: new Date(year + 1, 0, 1) },
    },
  },
  _sum: { amount: true },
});
```

---

### 5. 연도별 역할 기반 권한 검사

**커밋:** `eb2379c` fix: 연도별 역할(UserYearRole) 기반 권한 검사로 변경

**변경 내용:**
- 기존 `User.role` 대신 `UserYearRole` 테이블 기반 권한 검사
- 연도별로 다른 역할을 가질 수 있도록 지원

---

### 6. 기타

**커밋:** `12dd0e4` chore: 예산 세목명 비교 및 수정 스크립트 추가

- 세목명 불일치 데이터 확인용 임시 스크립트

---

## 커밋 내역

| 커밋 | 메시지 |
|------|--------|
| `8084899` | feat: 세목별 담당자 관리 예산금액 비율 표시 기능 추가 |
| `19efa0e` | feat: 세목별 담당자 관리 계층별 합계 및 배분율 표시 |
| `0d010ed` | feat: 세목별 사용금액을 지급완료 지출결의서 합계로 계산 |
| `7c4114c` | feat: budget-view 페이지 사용금액도 지급완료 지출결의서 합계로 계산 |
| `12dd0e4` | chore: 예산 세목명 비교 및 수정 스크립트 추가 |
| `eb2379c` | fix: 연도별 역할(UserYearRole) 기반 권한 검사로 변경 |

---

## 수정된 주요 파일

| 파일 | 변경 내용 |
|------|-----------|
| `app/admin/budget-managers/page.tsx` | 비율 표시, 계층별 합계 |
| `app/api/budget-details/year/route.ts` | 사용금액 실시간 계산 |
| `app/api/budget/hierarchy/route.ts` | 사용금액 실시간 계산 |
