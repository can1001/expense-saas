# 작업 일지 - 2026년 3월 28일

## 요약

- 로그인 아이디 기억하기 기능 추가
- 예산 목(subcategory) 데이터 수정: 임차보증금 → 임차보증금(상환)적립금
- 예산 데이터 수정 스크립트 추가 (추후 참고용)
- 양면 인쇄 개선: 빈 뒷면 추가, 문서/영수증 자동 분류 및 크기 구분

---

## 완료된 작업

### 1. 로그인 아이디 기억하기 기능 추가

**커밋:** `081fd7e`

로그인 페이지에 "아이디 기억하기" 체크박스를 추가하여 사용자 편의성 개선

**구현 내용:**
- 체크박스 선택 시 localStorage에 아이디 저장
- 다음 로그인 시 저장된 아이디 자동 입력
- 체크 해제 후 로그인하면 저장된 아이디 삭제

**수정된 파일:**
- `app/login/page.tsx` - 체크박스 UI 및 localStorage 로직 추가

**비밀번호 저장:**
- 브라우저의 자동완성 기능 활용 (`autoComplete="current-password"` 이미 적용됨)
- 보안상 localStorage에 비밀번호 직접 저장은 하지 않음

---

### 2. 예산 목(subcategory) 데이터 수정

**변경 내용:**
- 항(category): 적립금 (변경 없음)
- 목(subcategory): `임차보증금` → `임차보증금(상환)적립금`
- 세목(detail): 임차보증금(상환)적립금 (변경 없음)

**수정된 테이블 및 건수:**

| 테이블 | 업데이트 건수 |
|--------|-------------|
| BudgetSubcategory | 1건 |
| BudgetMaster | 0건 (해당 데이터 없음) |
| ExpenseItem | 1건 |
| SimpleExpenseItem | 0건 |

---

### 3. 예산 데이터 수정 스크립트 추가

**커밋:** `961d23f`

추후 예산 항목 수정 시 참고할 수 있는 스크립트 추가

**생성된 파일:**
- `scripts/update-budget-subcategory.ts` - 예산 목 수정 스크립트

**스크립트 기능:**
1. 변경 전 데이터 조회 (BudgetSubcategory, BudgetMaster, ExpenseItem, SimpleExpenseItem)
2. 4개 테이블 일괄 업데이트
3. 변경 후 데이터 검증

**실행 방법:**
```bash
npx tsx scripts/update-budget-subcategory.ts
```

---

## 수정/생성된 파일 (총 4개)

| 파일 | 변경 내용 |
|------|-----------|
| `app/login/page.tsx` | 아이디 기억하기 체크박스 추가 |
| `scripts/update-budget-subcategory.ts` | 예산 목 수정 스크립트 (신규) |
| `components/PrintableExpense.tsx` | 양면 인쇄 개선 - 빈 뒷면, 문서/영수증 분류 |
| `components/BulkPrintableExpenses.tsx` | 양면 인쇄 개선 - 빈 뒷면, 문서/영수증 분류 |

---

## 검증 완료

- [x] 아이디 기억하기 기능 구현 완료
- [x] 커밋 완료 (`081fd7e`)
- [x] 스크립트 실행 성공
- [x] BudgetSubcategory 업데이트 확인
- [x] ExpenseItem 업데이트 확인
- [x] 커밋 완료
- [x] 양면 인쇄 개선 구현 완료
- [x] 빌드 테스트 성공
- [x] 커밋 완료 (`32fe521`)

---

## 참고 사항

- `prisma/seed.ts` 파일의 453번 줄은 이미 올바른 값(`subcategory: '임차보증금(상환)적립금'`)으로 설정되어 있음
- 데이터베이스와 시드 파일 간 불일치가 있어 DB 직접 수정 진행
- 기존 지출결의서(ExpenseItem) 1건도 함께 업데이트됨

---

### 4. 양면 인쇄 개선 - 빈 뒷면 추가 및 문서/영수증 크기 구분

**커밋:** `32fe521`

지출결의서 양면 인쇄 시 건별로 분리되도록 개선

**구현 내용:**

#### 4.1 빈 뒷면 페이지 추가
- 첨부파일이 없는 지출결의서도 항상 뒷면 페이지 렌더링
- "(첨부서류 없음)" 텍스트 중앙 표시
- 양면 인쇄 시 각 지출결의서가 정확히 1장(앞뒤)으로 분리

#### 4.2 문서/영수증 자동 분류
이미지 가로세로 비율 기반 자동 분류:
- **문서**: 가로/세로 비율 ≥ 0.6 (A4 비율: 0.707), PDF 파일
- **영수증**: 가로/세로 비율 < 0.6 (세로로 긴 형태)

#### 4.3 차등 크기 출력
| 종류 | 개수 | 출력 방식 |
|------|------|-----------|
| 문서 1개 | max-height: 200mm | 전체 화면 |
| 문서 2개+ | max-height: 90mm | 상하 배치 |
| 영수증 1-2개 | max-height: 100mm | 2열 그리드 |
| 영수증 3-4개 | max-height: 90mm | 2x2 그리드 |
| 영수증 5개+ | max-height: 70mm | 3열 그리드 |

**수정된 파일:**
- `components/PrintableExpense.tsx` - 단건 인쇄
- `components/BulkPrintableExpenses.tsx` - 일괄 인쇄

**예상 결과 (양면 인쇄 시):**
```
[장 1 앞] 지출결의서 A 본문
[장 1 뒤] 지출결의서 A 첨부 (또는 "첨부서류 없음")
[장 2 앞] 지출결의서 B 본문
[장 2 뒤] 지출결의서 B 첨부
...
```
